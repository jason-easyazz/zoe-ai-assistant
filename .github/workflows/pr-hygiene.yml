# PR hygiene — size guard + overlap notice.
#
# Both exist to make "combine related work, submit when finished" cheap to follow.
#
# THRESHOLDS ARE FROM THE RESEARCH, NOT FROM OUR HABITS. The first cut set them above
# this repo's observed maximum, which encodes the current habit rather than the point
# where review actually degrades. The evidence (SmartBear/Cisco ~2500 PRs; Google):
# defect detection is ~87% at 1-100 changed lines, ~65% at 301-600, and ~28% at 1000+.
# 200 lines is the target, 400 the ceiling past which detection measurably falls.
#
# Our own distribution (40 merged PRs, 2026-07): median 246 lines, p75 401, p90 653,
# max 975. So the median is healthy but the top quartile is already in the degraded
# band — the warn threshold is meant to fire there. That is the signal, not noise.
#
# The hard fail also sits below Greptile's ~50-file silent skip: past that a PR gets NO
# review while still costing a credit, which is worse than being blocked.
name: pr-hygiene

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review, labeled, unlabeled]

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: pr-hygiene-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  size-and-overlap:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v9
        with:
          script: |
            const pr = context.payload.pull_request;
            if (pr.draft) { core.info('draft — hygiene checks deferred until ready'); return; }

            // Files that legitimately move in bulk and say nothing about review burden.
            const GENERATED = [
              /^docs\/knowledge\/flag-inventory\.(md|json)$/,
              /^services\/zoe-ui\/dist\/lib\//,
              /package-lock\.json$/,
            ];
            const OVERRIDE = 'oversized-ok';
            const WARN  = { files: 10, lines: 400 };   // research ceiling — detection falls past here
            const FAIL  = { files: 30, lines: 1000 };  // ~28% detection; also under Greptile's ~50-file skip

            const files = await github.paginate(github.rest.pulls.listFiles, {
              owner: context.repo.owner, repo: context.repo.repo,
              pull_number: pr.number, per_page: 100,
            });
            const counted = files.filter(f => !GENERATED.some(re => re.test(f.filename)));
            const nFiles = counted.length;
            const nLines = counted.reduce((a, f) => a + f.additions + f.deletions, 0);
            const skipped = files.length - nFiles;
            core.info(`counted ${nFiles} files / ${nLines} lines (${skipped} generated excluded)`);

            // ── Overlap notice: is another OPEN PR already touching these files? ──
            // This is the automatable half of "combine related work before review".
            // Reviews are billed per PR and per push, so two PRs over the same files
            // pay twice and then conflict. Advisory only — it reports, never blocks.
            const others = await github.paginate(github.rest.pulls.list, {
              owner: context.repo.owner, repo: context.repo.repo, state: 'open', per_page: 100,
            });
            const mine = new Set(counted.map(f => f.filename));
            const overlaps = [];
            for (const o of others) {
              if (o.number === pr.number || o.draft) continue;
              const of = await github.paginate(github.rest.pulls.listFiles, {
                owner: context.repo.owner, repo: context.repo.repo,
                pull_number: o.number, per_page: 100,
              });
              const shared = of.map(f => f.filename).filter(f => mine.has(f));
              if (shared.length) overlaps.push({ number: o.number, title: o.title, shared });
            }

            const lines = [];
            if (overlaps.length) {
              lines.push('**Overlapping open PRs** — consider combining before review:');
              for (const o of overlaps) {
                lines.push(`- #${o.number} (${o.title}) shares: ${o.shared.slice(0, 5).join(', ')}${o.shared.length > 5 ? ` +${o.shared.length - 5} more` : ''}`);
              }
              lines.push('');
              lines.push('Two PRs over the same files are billed twice by every reviewer, and the second inherits the first\'s conflicts. Landing them as one branch is cheaper and reviews better.');
            }

            // >= not >: the documented limit is 1000 lines, so exactly 1000 must be AT the
            // limit, not one line under it. `> 1000` silently allowed the boundary case.
            const over = (t) => nFiles >= t.files || nLines >= t.lines;
            const hasOverride = pr.labels.some(l => l.name === OVERRIDE);

            // The override must silence the WARN branch too. Otherwise a PR that exceeds
            // both thresholds keeps posting "Getting large" on every push despite carrying
            // an explicit, deliberate exemption — nagging about a decision already made.
            if (hasOverride) {
              core.info(`PR #${pr.number}: ${OVERRIDE} present — size messaging suppressed`);
            } else if (over(FAIL)) {
              lines.push('', `**Too large:** ${nFiles} files / ${nLines} lines (limit ${FAIL.files} / ${FAIL.lines}). At 1000+ changed lines reviewers find roughly 28% of defects.`,
                '', `Split it, or add the \`${OVERRIDE}\` label if it genuinely cannot be split. Note Greptile silently SKIPS PRs over ~50 files — past that you get no review at all while still paying for it.`);
            } else if (over(WARN)) {
              lines.push('', `**Getting large:** ${nFiles} files / ${nLines} lines. Past ~400 changed lines defect detection drops from ~87% to ~65% and keeps falling (SmartBear/Cisco, Google). Not blocking — but this is the point where splitting pays.`);
            }

            // Sticky: edit our previous notice rather than posting a new one on every
            // push. Observed on this PR — two near-identical "Getting large" comments
            // after two syncs. Noise trains people to ignore the check.
            const MARK = '<!-- pr-hygiene -->';
            // A cleared condition must CLEAR the notice. Once a PR shrinks, the overlap
            // lands, or `oversized-ok` is applied, `lines` is empty — and simply skipping
            // would leave an old warning on screen describing a problem that no longer
            // exists. A stale warning is worse than none: it is wrong, and it is trusted.
            if (!lines.length) {
              const stale = (await github.paginate(github.rest.issues.listComments, {
                owner: context.repo.owner, repo: context.repo.repo,
                issue_number: pr.number, per_page: 100,
              })).find(c => c.body?.includes(MARK));
              if (stale) {
                await github.rest.issues.updateComment({
                  owner: context.repo.owner, repo: context.repo.repo,
                  comment_id: stale.id,
                  body: `PR hygiene: clear — ${nFiles} files / ${nLines} lines, no overlapping open PRs.\n${MARK}`,
                });
                core.info(`PR #${pr.number}: hygiene notice cleared`);
              }
            }
            if (lines.length) {
              const body = `${lines.join('\n')}\n${MARK}`;
              const existing = (await github.paginate(github.rest.issues.listComments, {
                owner: context.repo.owner, repo: context.repo.repo,
                issue_number: pr.number, per_page: 100,
              })).find(c => c.body?.includes(MARK));
              if (existing) {
                await github.rest.issues.updateComment({
                  owner: context.repo.owner, repo: context.repo.repo,
                  comment_id: existing.id, body,
                });
              } else {
                await github.rest.issues.createComment({
                  owner: context.repo.owner, repo: context.repo.repo,
                  issue_number: pr.number, body,
                });
              }
            }
            if (over(FAIL) && !hasOverride) {
              core.setFailed(`PR too large: ${nFiles} files / ${nLines} lines`);
            }
