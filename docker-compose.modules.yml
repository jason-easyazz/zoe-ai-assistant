# ============================================================
# AUTO-GENERATED FILE - DO NOT EDIT DIRECTLY
# ============================================================
# This file is generated by tools/generate_module_compose.py
# Edit config/modules.yaml to enable/disable modules, then regenerate
# Generated: /home/zoe/assistant
# ============================================================

services:
  zoe-agent0:
    image: agent0ai/agent-zero:v0.9.7
    container_name: zoe-agent0
    ports:
    - 50001:80
    environment:
    - ALLOWED_ORIGINS=*://localhost:*,*://127.0.0.1:*,*://0.0.0.0:*,*://192.168.1.*:*
    volumes:
    - agent0-data:/app/data
    - agent0-memory:/app/memory
    - agent0-logs:/app/logs
    networks:
    - zoe-network
    restart: unless-stopped
  agent-zero-bridge:
    build: .
    container_name: agent-zero-bridge
    ports:
    - 8101:8101
    volumes:
    - .:/app
    - ../../data:/app/data
    environment:
    - PYTHONUNBUFFERED=1
    - AGENT_ZERO_URL=http://zoe-agent0:80
    - AGENT_ZERO_API_KEY=${AGENT_ZERO_API_KEY:-}
    - A0_PERSISTENT_RUNTIME_ID=${A0_PERSISTENT_RUNTIME_ID:-c13febd01bf518de389462d4d48b2285}
    - DATABASE_PATH=/app/data/zoe.db
    - SAFETY_MODE=${AGENT_ZERO_SAFETY_MODE:-grandma}
    networks:
    - zoe-network
    depends_on:
    - zoe-agent0
    restart: unless-stopped
    healthcheck:
      test:
      - CMD
      - curl
      - -f
      - http://localhost:8101/health
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
  qd-game:
    build: ./modules/questionable-decisions
    container_name: zoe-qd-game
    ports:
    - 8200:8200
    volumes:
    - ./modules/questionable-decisions/decks/custom:/app/decks/custom
    environment:
    - PYTHONUNBUFFERED=1
    networks:
    - zoe-network
    restart: unless-stopped
    healthcheck:
      test:
      - CMD
      - curl
      - -f
      - http://localhost:8200/health
      interval: 30s
      timeout: 10s
      retries: 3
  jag-board:
    build: ./modules/jag-board
    container_name: zoe-jag-board
    ports:
    - 8201:8201
    volumes:
    # Mount full module so static/CSS/JS edits apply without rebuilding the image
    - ./modules/jag-board:/app
    environment:
    - PYTHONUNBUFFERED=1
    networks:
    - zoe-network
    restart: unless-stopped
    healthcheck:
      test:
      - CMD
      - curl
      - -f
      - http://localhost:8201/health
      interval: 30s
      timeout: 10s
      retries: 3
  orbit:
    build: ./modules/orbit
    container_name: zoe-orbit
    ports:
    - 8202:8202
    volumes:
    - ./modules/orbit/data:/app/data
    environment:
    - PYTHONUNBUFFERED=1
    networks:
    - zoe-network
    restart: unless-stopped
    healthcheck:
      test:
      - CMD
      - curl
      - -f
      - http://localhost:8202/health
      interval: 30s
      timeout: 10s
      retries: 3
  music-assistant:
    image: ghcr.io/music-assistant/server:stable
    container_name: zoe-music-assistant
    network_mode: host
    volumes:
      - ./data/music-assistant:/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8095/info"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

  zoe-database:
    image: pgvector/pgvector:pg17
    container_name: zoe-database
    env_file:
      - ./services/zoe-data/.env
    environment:
      POSTGRES_DB: zoe
      POSTGRES_USER: zoe
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "5432:5432"
    volumes:
      - zoe-database-data:/var/lib/postgresql/data
    networks:
      - zoe-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U zoe"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ── Multica — A2A board visibility (opt-in: ZOE_MULTICA=true) ───────────────
  # Multica shares the zoe-database container (already running); uses its own
  # 'multica' database. Bootstrap: docker exec zoe-database psql -U zoe -c
  # 'CREATE DATABASE multica OWNER zoe;'  then set ZOE_MULTICA=true in .env.
  multica-backend:
    image: ghcr.io/multica-ai/multica-backend:latest
    container_name: zoe-multica-backend
    ports:
      - "8080:8080"
    environment:
      DATABASE_URL: "postgresql://zoe:${POSTGRES_PASSWORD}@host.docker.internal:5432/multica?sslmode=disable"
      PORT: "8080"
      # development required — dev verification code is silently ignored when APP_ENV=production
      APP_ENV: development
      JWT_SECRET: "${MULTICA_JWT_SECRET}"
      MULTICA_DEV_VERIFICATION_CODE: "${MULTICA_DEV_CODE}"
      FRONTEND_ORIGIN: "http://localhost:3000"
      MULTICA_APP_URL: "http://localhost:3000"
      ALLOW_SIGNUP: "true"
      LOCAL_UPLOAD_DIR: "/data/uploads"
      LOCAL_UPLOAD_BASE_URL: "http://localhost:8080"
      # Uncomment after creating Multica OIDC app in Authentik:
      # OAUTH_CLIENT_ID: ${MULTICA_OIDC_CLIENT_ID}
      # OAUTH_CLIENT_SECRET: ${MULTICA_OIDC_CLIENT_SECRET}
      # OAUTH_DISCOVERY_URL: http://authentik-server:9000/auth/application/o/multica/.well-known/openid-configuration
    volumes:
      - multica-uploads:/data/uploads
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - zoe-network
    restart: unless-stopped
    profiles:
      - multica
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

  multica-web:
    image: ghcr.io/multica-ai/multica-web:latest
    container_name: zoe-multica-web
    ports:
      - "3000:3000"
    environment:
      NEXT_PUBLIC_API_URL: "http://localhost:8080"
      NEXT_PUBLIC_WS_URL: "ws://localhost:8080"
    networks:
      - zoe-network
    restart: unless-stopped
    profiles:
      - multica
    depends_on:
      - multica-backend

  authentik-redis:
    image: redis:7-alpine
    container_name: authentik-redis
    command: --save 60 1 --loglevel warning
    volumes:
      - authentik-redis-data:/data
    networks: [zoe-network]
    restart: unless-stopped
    profiles: ["authentik"]

  authentik-server:
    image: ghcr.io/goauthentik/server:2024.12
    container_name: authentik-server
    command: server
    environment:
      AUTHENTIK_REDIS__HOST: authentik-redis
      AUTHENTIK_POSTGRESQL__HOST: zoe-database
      AUTHENTIK_POSTGRESQL__USER: zoe
      AUTHENTIK_POSTGRESQL__PASSWORD: ${POSTGRES_PASSWORD}
      AUTHENTIK_POSTGRESQL__NAME: authentik
      AUTHENTIK_SECRET_KEY: ${AUTHENTIK_SECRET_KEY}
      AUTHENTIK_WEB__PATH_PREFIX: "/auth"
      AUTHENTIK_ERROR_REPORTING__ENABLED: "false"
      AUTHENTIK_BOOTSTRAP_EMAIL: ${AUTHENTIK_BOOTSTRAP_EMAIL}
      AUTHENTIK_BOOTSTRAP_PASSWORD: ${AUTHENTIK_BOOTSTRAP_PASSWORD}
      AUTHENTIK_BOOTSTRAP_TOKEN: ${AUTHENTIK_BOOTSTRAP_TOKEN}
    volumes:
      - authentik-media:/media
      - authentik-templates:/templates
    networks: [zoe-network]
    depends_on:
      - zoe-database
      - authentik-redis
    restart: unless-stopped
    profiles: ["authentik"]

  authentik-worker:
    image: ghcr.io/goauthentik/server:2024.12
    container_name: authentik-worker
    command: worker
    environment:
      AUTHENTIK_REDIS__HOST: authentik-redis
      AUTHENTIK_POSTGRESQL__HOST: zoe-database
      AUTHENTIK_POSTGRESQL__USER: zoe
      AUTHENTIK_POSTGRESQL__PASSWORD: ${POSTGRES_PASSWORD}
      AUTHENTIK_POSTGRESQL__NAME: authentik
      AUTHENTIK_SECRET_KEY: ${AUTHENTIK_SECRET_KEY}
      AUTHENTIK_WEB__PATH_PREFIX: "/auth"
      AUTHENTIK_BOOTSTRAP_EMAIL: ${AUTHENTIK_BOOTSTRAP_EMAIL}
      AUTHENTIK_BOOTSTRAP_PASSWORD: ${AUTHENTIK_BOOTSTRAP_PASSWORD}
      AUTHENTIK_BOOTSTRAP_TOKEN: ${AUTHENTIK_BOOTSTRAP_TOKEN}
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - authentik-media:/media
      - authentik-certs:/certs
    networks: [zoe-network]
    depends_on:
      - authentik-redis
      - zoe-database
    restart: unless-stopped
    profiles: ["authentik"]

networks:
  zoe-network:
    name: zoe-network
    external: true
volumes:
  agent0-data: null
  agent0-memory: null
  agent0-logs: null
  zoe-database-data: null
  multica-uploads: null
  authentik-redis-data: null
  authentik-media: null
  authentik-templates: null
  authentik-certs: null
