# Retired Raspberry Pi Docker override.
#
# Raspberry Pi 5 is no longer an active server target. Touch-panel/voice-device
# setup lives under scripts/setup/touchscreen and scripts/setup/*voice*; the
# server-side LLM/STT containers referenced by this overlay were retired.

services: {}
