# Retired Jetson Docker override.
#
# The active Jetson runtime uses host-native systemd user services for zoe-data,
# llama-server, OpenClaw, Hermes, and Kokoro TTS. GPU/runtime flags live in those
# unit files, not Docker Compose. This no-op overlay is kept so old documented
# compose commands validate without reviving retired containers.

services: {}
