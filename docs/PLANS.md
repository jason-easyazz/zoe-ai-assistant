---
type: plans-index
audience: human-first (Jason) + all agents
---
# Zoe Plans 📋

**What we're actually building, in plain English, with status.** One line of goal +
a link to the detail + where it's up to. Mark ✅ when done so nothing lingers half-finished.

**Status key:** 📝 planned · 🔨 active · ✅ done · 🗄️ parked

> **For agents:** this is the human-readable index. Detailed/executable plans live in
> `docs/architecture/*.md` (and agent memory). When you finish a step, update the
> status here AND in the detailed plan. New ideas start in [IDEAS.md](IDEAS.md).

---

## 🔨 Active

### Music discovery (hidden digarr engine) + per-user listening journal
- **Goal:** Zoe finds the family new music — digarr runs as a batch-only ephemeral container against the local Gemma brain (labs/digarr-spike/ verdict), seeded from a fully-local per-user listening journal; results land as the "Zoe Discovery" MA playlist ("play my discovery playlist" just works). Nothing leaves the house (no cloud scrobblers).
- **Now (PR-1):** journal (`music_play_history`, alembic 0018; initiated-event capture at the play choke points + observed-events poll, guest-fallback attribution via `music_history.resolve_music_user`), taste seed (journal-first, MA recently-played/play_count fallback), batch script (`scripts/maintenance/music_discovery_batch.py`, memory + brain-idle gated), JSON→MA playlist bridge, discovery-playlist intent alias, weekly cron behind `ZOE_MUSIC_DISCOVERY` (default OFF — operator flips after a verified manual run: `python3 scripts/maintenance/music_discovery_batch.py` in an idle voice window).
- **Next (PR-2):** per-user discovery ("Zoe Discovery — <name>" playlists from `taste_seed(user_id=…)`, digarr multi-user or per-run configs); spoken "I found N new albums" summary hook; speaker-ID attribution rides the Samantha roadmap.

### Samantha evolution — the post-memory frontier (ears, voice-first initiative, presence)
- **Goal:** with memory (pillar 1) delivered, close the remaining Samantha gaps in leverage order: conversation-grade voice (barge-in + real turn detection + streamed TTS), Zoe **speaking first** (proactive → spoken via `panel_announce` + a presence check), a **RAM-reclamation workstream** (≥2 GB; the box gates everything), prosody **emotion sensing** (small SER model feeding the existing `emotional_moment` pipeline), speaker-ID enable, attributed ambient capture, closing the self-evolution loop once, and a first surface that leaves the house (Telegram voice notes).
- **Detail / executable plan:** [`docs/architecture/samantha-evolution-plan.md`](architecture/samantha-evolution-plan.md) (workstreams W0–W8 + the §9 OS-horizon W9–W16: digital-life ingestion/email triage, Zoe's own first-person thread + gated persona evolution, expressive delivery, the remote direct link, interface agency (control/compose/author the touch screen), soul backup/restore drills, the untrusted-content trust boundary, the weekly Samantha scoreboard, one-conversation-across-surfaces, closing the orphaned feedback loop; gates, dependency graph, prior-art grounding §8, NEXT ACTION pointer; cheap-model execution packets in [`samantha-evolution-packets.md`](architecture/samantha-evolution-packets.md)). Grounded in the 2026-07-06 capability audit, the ambient-voice ADR spike verdict, and [`docs/knowledge/memory-pressure-profile.md`](knowledge/memory-pressure-profile.md).
- **Now (🔨):** **W1.1 barge-in + W1.2 Smart Turn v3 ✅ SHIPPED + LIVE** (#1051 Silero VAD + full-duplex barge + Smart Turn endpointing; #1081 real-voice barge gate fix 6/6; prod flags ON per #1082). Remaining W1: **W1.3** sentence-streamed TTS in the LiveKit lane + the **M3/M4** latency/RAM measurements. **2026-07-07 speed pass on the REAL panel lane (Pi daemon → turn_stream):** conversation mode live (#1086 opener fast-path, #1092 courtesy-edge enders, #1097 dedicated barge monitor with instant TTS kill, live-tuned); semantic-router **expert dispatch ACTIVE** (weather/time answered ~0.9-1.1s end-to-end, was 5.6s); **thinking-filler chain #1106→#1113→#1116** (filler must race voice_command AND the lazy first body chunk, and only an audio-bearing frame ends the race — a text-only processing_ack was silently defeating it): open-chat first audio now **~2.2s** (spoken "One sec." filler; real answer streams behind it), was 5-11s of silence. Optional next notch: `ZOE_PROCESSING_ACK_AUDIO_PATHS` (wavs pre-generated in `data/voice_acks/`, lab-verified) makes the existing processing-ack audible at ~0.6s — operator env flip + restart. **2026-07-08 filler retired by speed:** Seam-A NDJSON streaming consumed in `zoe_flue_client` (#1148, `ZOE_FLUE_STREAM_ENABLED=1` live — the sidecar streamed since #971 but the client waited on `?wait=result`): warm chat first-audio **2.82s with no filler** (threshold 2.5s), total 5.6s (was 5-11s of silence). (llama-server `--parallel 2` was tried for prefix-cache retention but REVERTED: `--ctx-size 8192 --parallel 2` = 4096/slot < the ~4.4k-token persona prompt → context-overflow on long turns; box lacks RAM for the `--ctx-size 16384` fix. Back on `--parallel 1`; the durable de-swap sudo one-liner remains the real brain-latency lever.) Also live 2026-07-07/08: VAD endpointing on the Pi recorder (#1121, tail 1.5s→1.0s tuned), barge monitor playback-gate (#1129) + threshold 0.75, and the turn re-POST duplicate-write fix (#1137, daemon + 10s server replay guards). **2026-07-11/13 TTS caching arc ✅:** first-turn-of-day spoken greeting (#1228), Kokoro exact-text LRU + persistent frequency-ranked cache (#1229/#1230/#1232), and segment-stitched time/weather replies **firing on real panel turns** (#1268 + #1272 — whole-degree spoken temps in BOTH weather producers (`skybridge_service` builds the panel reply, not `intent_router`) + real reply formats incl. feels-like; live turns `providers=['stitch']`: weather ~0.6s, time ~0.4s vs ~2.6s full synth). **W0 capture pipeline ✅ FULLY verified (2026-07-13)** — chat/Telegram lane proven on organic traffic (2026-07 memory QA), and the spoken-panel positive control now passes: it exposed three stacked transcript-save bugs (bad `routers.chat` lazy import, missing `chat_sessions` FK parent, streaming lane never persisting the reply), fixed in #1282 — live spoken turn → stamped user+assistant `chat_messages` rows under the panel-bound user. Still open: **W3.1** ccd-cli fleet cleanup (operational, ~3.6 GB swap).

### Samantha-grade memory
- **Goal:** local, fast, **no-nightly** companion memory — tell it in the morning, reference it in the afternoon; recalls the emotional thread; feels like Samantha.
- **Detail / executable plan:** [`docs/architecture/zoe-memory-samantha-buildplan.md`](architecture/zoe-memory-samantha-buildplan.md) (has the NEXT ACTION pointer + checklist).
- **Now:** 1a (idle-consolidation engine, #771) ✅ + 1b (per-turn user, #775) ✅ merged behind `ZOE_IDLE_CONSOLIDATION_ENABLED`; 1c live→idle→store→recall loop **proven on real Telegram/panel traffic** during the **2026-07 memory-hardening arc ✅** — full behavioral QA review ([`docs/architecture/memory-qa-review-2026-07.md`](architecture/memory-qa-review-2026-07.md)) + the #1242–#1266 fix chain, all LIVE: coreference/possessive capture + corrections-supersede-on-ingest (#1242/#1260/#1261), cold-start recall warmup (#1244), Telegram `/new` session reset (#1246), memory-packet authority over conversational denials (#1248), qualified relationship anchors at capture (#1251), db-pool leak outage fix + pool hardening/503s/gauges (#1258/#1262), correction-garbling + negation fixes (#1264), and the **contacts pipeline** — memories/chats → propose-then-confirm contact offers on any conversational turn (#1265/#1266, live-proven end-to-end). **QA follow-through complete (2026-07-13):** F9 cross-writer supersession (#1280), F13 honest save confirmations (#1281), F14 entity-scoped forget (#1281), and the W0 voice-transcript persistence fix (#1282) all merged; jason's store cleaned of pre-fix misattribution rows. **Supersession-hardening wave (2026-07-13, all LIVE-verified on the real Telegram lane with demo users):** the 6 tests failing on main were 5 stale expectations + 1 real bug — the entity guard treated name-attribute VALUES ("…name is Neil", "wife is Sarah", multi-word/punctuated names) as third-person anchors, so kinship-name corrections could never supersede (#1286/#1288); the live correction test then caught reconcile's 2s search fail-open silently degrading corrections to ADD under load — reconcile now searches patiently at 15s with WARNING-level degradation (#1290); compact person rows ("Delia: March 15") gained entity-keyed supersession via stored `pattern_type` + `MemoryService.list_by_entity` (their "Name: value" shape has no parseable attribute for text reconciliation), plus discourse openers ("actually …") no longer mint junk persons (#1294); and the live F14 forget test exposed + fixed two leaks — the forget turn itself was fact-mined (minting junk that resurrected the name) and the pending contact offer survived the forget (#1295). End state proven live: back-to-back Telegram correction supersedes on every writer lane, and "forget everything about X" leaves zero trace (rows archived, offer withdrawn, packet clean) with other people untouched. Remaining by design: F15 (relationship edges, flags/RAM-gated) and the pinned **forget-tombstone** idea (IDEAS.md — async extraction can land a straggler row seconds after a forget).

### Ambient voice (VISION pillar 2 — always-there presence)
- **Goal:** continuous, wake-free, **interruptible** voice conversation (barge-in + real turn detection), and — adjacent — **phone calls**. The next Samantha frontier after memory.
- **Decision:** [`docs/adr/ADR-ambient-voice-framework.md`](adr/ADR-ambient-voice-framework.md) — landscape researched (Pipecat / LiveKit / TEN / full-duplex S2S / Jambonz / Wyoming). TEN rejected (Agora cloud dep); S2S models (Moshi) parked (replace the brain rock + need 24 GB VRAM). **Spike RUN 2026-07-05 (#1045): PARTIAL GO, verdict shifted to the fallback** — Moonshine not native in Pipecat, numpy-2/Jetson-torch ABI conflict, and no RAM headroom for a parallel stack → **borrow Smart Turn v3 standalone + hand-build barge-in on the existing LiveKit agent**; full Pipecat migration only if that proves insufficient. (The interim #1056 migration-plan doc was retired once #1051 shipped the work it planned; its surviving pieces — Pipecat re-open triggers, A3 gate bars — live in the evolution plan.)
- **Now (🔨):** the fallback is **executed and LIVE** — barge-in + Silero VAD + Smart Turn v3 endpointing shipped as W1.1/W1.2 of the [`samantha-evolution-plan.md`](architecture/samantha-evolution-plan.md) (#1051; real-voice barge gate fixed 6/6 in #1081; prod flags ON, #1082). M1 (barge quality) verified on real voice. **Remaining:** sentence-streamed TTS in conversation mode (W1.3) + the deferred M3 latency / M4 RAM measurements (W1.4). Pipecat stays parked with explicit re-open triggers (plan §2). **Phone (SIP) + Wyoming room satellites are later, separable increments (W8).**

### Agent + tooling readiness (for Jason to work on Zoe remotely)
- **Goal:** Omnigent + Claude Code + Codex + Cursor all set up correctly and **actually using** the repo's tools (Serena, codebase-memory, opensrc, Greptile/greploop, Dox), with a shared command center (this) so ideas/plans don't scatter.
- **Now:** audit ✅ (#774, `docs/agent-setup-audit.md`); opensrc populated ✅ (#773); **Claude Code** wired ✅ (#787 — `CLAUDE.md`+committed `.mcp.json`), **Codex** ✅ (#788 — `.codex/config.toml`), **Cursor** ✅ (#786 — fixed `.cursor/mcp.json`); **Omnigent container code-intel** ✅ (#792 committed the mounts; **applied + verified live 2026-06-26** — recreated the container, serena+codebase-memory+opensrc all resolve in-container, harnesses launch with `--dangerously-skip-permissions` so the project MCP auto-loads); **shared knowledge bundle populated** ✅ (#855 — runtime-topology/voice-pipeline/merge-and-deploy promoted from private memory into `docs/knowledge/` so the whole fleet reads the same ground-truth). **Remaining (🔨):** tool-use enforcement verification across agents (AGENTS.md start-of-task checklist landed; confirm each harness honours it).

### Flue convergence — recreate the harness on Flue, then retire Multica/Hermes/OpenClaw
- **Goal:** one engine (Pi) hosted by Flue owns the brain lane + the autonomous engineering harness; Multica/Hermes/OpenClaw are retired **by recreation** — each capability is rebuilt on Flue, gate-proven, then deleted (never deleted first).
- **Detail / executable plan:** [`docs/architecture/zoe-flue-integration.md`](architecture/zoe-flue-integration.md) — seams (§3), zero-regression gates (§4), phases (§6), and the per-capability **retirement inventory** (§8: what Multica/Hermes/OpenClaw each do today → the Flue recreation → the retire-gate).
- **§8.1 retire-gate spec (Multica):** [`docs/architecture/multica-retirement-gates.md`](architecture/multica-retirement-gates.md) — code-verified capability inventory (poll loop, admission/dispatch, phase chain, PR closeout, worktrees, autopilots) with per-capability proof gates, rollback (Multica installed-but-idle until each gate holds), sequencing (autopilots first), and the Forbidden list.
- **Now:** Phase 0 (harness on Flue) ✅ #858 (+1.5 ms voice). **Brain lane (phases 1–2 of the brain campaign = detail-doc Phases 2–3) ✅ delivered:** #947 (event-loop fork-deadlock fix — 3.9-day `/api/memories/for-prompt` outage; now 18–66 ms), #915/#939 (recall reliability 97% + turn cap), #944 (non-mutating dry-run preflight), #960 (`reminder_list` direct executor), #904 (`ZOE_BRAIN_BACKEND=flue` seam, default OFF), #952 (progressive tool disclosure 11→3 schemas + systemd unit template + runbook), #965 (activator fallback hardening), #971 (Seam-A sentinel streaming). E2E-verified through the seam; ~2× faster than the prod core on pure chat. **Voice-parity gate RUN 2026-07-03: latency clear win, quality ~parity — median 2.5s vs 5.3s (clean, unconfounded); raw quality flue 41/44 vs prod 38/44 OVERSTATES flue (identity confound: prod ran as guest w/ contaminated memory, flue as empty-store test user). Removing the 4 identity-decided prompts → flue 37/40 (92.5%) vs prod 38/40 (95%), prod marginally ahead on quality. Clean quality re-run needs an authenticated test user (blocked by zoe-auth provisioning).** On-box LANDING checks PASS (sentinel stream; activator 7/10 trigger-free, zero fabrication; recall 96.9%). **CUT OVER TO PRODUCTION 2026-07-03** (operator-authorized): `ZOE_BRAIN_BACKEND=flue` live, the sidecar a supervised systemd user service (token auth, virtual sandbox), routing proven; instant env rollback. Voice-degrading blockers cleared pre-cutover: "I don'm" token corruption (#991, temperature parity 0.5) + in-session context recall (#988). Post-cutover write-path fixes ✅ **CLOSED 2026-07-06** — the #960/#993 mcporter-None class was root-caused one level deeper: a stale rotated `POSTGRES_URL` baked into `~/.mcporter/mcporter.json` pre-empted `bootstrap_runtime_env()`'s canonical `.env` load, so every spawned `mcp_server.py` failed DB auth and died mid-call. Baked credential removed (rotation-proof), `run_stdio_server` exits loudly, `_run_mcporter` moved off the on-loop fork (#947 class); residual fallback intents verified working (direct executors #993 + calendar/note/reminder/journal/people remain first-choice). Multi-user identity ✅ **DONE** (#998/#1000/#1001, live) — shipped as AbortSignal-keyed WeakMap + trusted ` zoe-uid:` envelope (NOT AsyncLocalStorage, proven broken through `?wait=result`); see `labs/flue-zoe-brain/src/request-identity.ts`. Known open: (a) quality marginally below the old core (92.5% vs 95%, confound-corrected) — traded for the ~2× latency win; clean re-run blocked on an authenticated test user (zoe-auth provisioning); (b) tool coverage 12 → 20 via Waves 1–3 (remainder deliberately cut per [`docs/knowledge/flue-cutover-tool-cut-list.md`](knowledge/flue-cutover-tool-cut-list.md), signed off 2026-07-03). Phase 1 (Telegram front door) ✅ **done + live-hardened** — bot built (#870), re-slotted through `/api/chat` with the `channel` tag, self-service `/start` deep-link account linking, `/new` session reset (#1246), and a health watchdog for the silent poll-loop death mode (#1233). **NEXT:** §8.1 Multica recreation, gates before any deletion.

### Touch / Chat UI — "window into Zoe" (foundation-first)
- **Goal:** the touch panel + chat page become a real window into Zoe — type/speak anything, get working interactive components (not prose). DeerFlow-grade, dynamic from basic (calendar) to advanced (Pi builds something new).
- **Order (foundation before features — the panel froze invisible on a hung turn; don't bolt features on a brittle shell):** **1. Resilience → 2. Consolidation → 3. Brain-serves-experience.**
- **Now:** **1. Resilience ✅** (PR #770 merged+deployed+verified-on-panel: turn-stall watchdog, guaranteed UI recovery, + root-cause orb fix — `sky-ambient-clock` was overloaded as a body-flag AND element class, fading the whole UI). Foundations also merged: #766 (Skybridge calendar/list voice+touch loop), #767 (chat tool-activity + interactive-component renderer), #769 (deploy health-check fix). **NEXT: 2. Consolidation** — retire the legacy `dashboard.html`/dual-stack ([`skybridge-cutover-plan.md`](architecture/skybridge-cutover-plan.md), partly done), converge the **4** card producers onto the one validated component contract, tame the z-index/CSS sprawl. Then **3.** Wave A (brain tool calls → `zoe.component` cards). Component contract + `zoe.component` AG-UI event already shipped.

### Zoe's voice — user-facing voice selection (touch panel)
- **Goal:** pick Zoe's speaking voice from the panel (Settings → "Zoe's Voice" card): live catalogue from the voices bin, tap-to-set (persisted household pref, `app_settings.tts_voice`), per-voice Preview played on the panel. Custom `zoe_*` blends (labs/kokoro-voice-blend, #1274) appear once the operator installs the augmented bin.
- **Now:** 🔨 PR open (branch `feature/voice-settings`) — backend pref + catalogue + preview endpoints, skybridge `voice` intent + `voice_settings` card, voice-scoped sidecar phrase-cache keys (fixes the stale-voice cache bug). Operator enable steps for `zoe_*`: [`docs/knowledge/voice-pipeline.md`](knowledge/voice-pipeline.md) §Voice selection.

### Tech-debt remediation (from the 2026-07-04 architecture review)
- **Goal:** address the concentrated, addressable debt a senior-dev review found — "extract seams from a healthy monolith," not a rewrite. Ordered by leverage-per-risk.
- **Detail / executable plan:** [`docs/architecture/tech-debt-remediation-plan.md`](architecture/tech-debt-remediation-plan.md) (sequenced small PRs, waves + gates).
- **Now:** 🔨 in progress. **Every claim verified against the live tree/host 2026-07-04 (3 read-only sub-agents); the plan doc has the corrections.** **Wave 1** (docs/hygiene) ✅ **merged** — #1008 (brain docs → live `flue` sidecar, not the dormant `services/zoe-core`), #1009 (drift sweep), #1010 (untrack runtime artifacts). **Wave 2** CI gate ✅ **merged** — #1013 (marker-based `ci_safe` selection, NOT blanket `testpaths`) + #1011 (post-deploy self-hosted full-dir pytest); the recurring silent-skip is now closed by the **two-lane mechanism** (GitHub `-m ci_safe` + self-hosted catch-all). **Wave 3 — relationship-memory roadmap (the corrected `people_relate` lineage): ✅ merged but DARK (all behind flags, default OFF).** ADR #1022 (relational property graph on Postgres, not Graphiti) → temporal edges migration `0015` #1024 → recursive-CTE graph traversal `GET /people/{id}/graph`, current-edge-only #1025/#1029 → person-merge / entity-resolution #1036 → end-to-end lab-proof (all 3 flags on, isolated SQLite) #1044. **Go-live is an operator step — [`docs/knowledge/relationship-memory-flag-enable.md`](knowledge/relationship-memory-flag-enable.md) #1046:** migrate `0015` first → flip `ZOE_TEMPORAL_RELATIONSHIPS_ENABLED` / `ZOE_RELATIONSHIP_GRAPH_ENABLED` / `ZOE_PERSON_MERGE_ENABLED` incrementally behind the `~/.zoe-voice-samples` replay gate → verify on a demo user → roll back with the flags, never a schema downgrade. **Remaining (🔨/📝):** increment-4 confidence/admission-gating of relationship *writes* (open); the CI test-isolation leak (2 graph-endpoint tests + `test_zoe_core_client` deselected on the fast lane); **Wave 3 Tier 2** (extract shared calendar/list/people services, one-aggregate-per-PR, replay-gated); **Wave 4** god-files/typed config/fence harness.
- **Wave 4 execution packet (2026-07-06):** [`docs/architecture/chat-split-and-typed-config-plan.md`](architecture/chat-split-and-typed-config-plan.md) — sequenced per-PR packet for the `chat.py` SSE-protocol/routing-policy split (kills the duplicate `use_flue_brain()` first) + the `typed_env` typed-config migration (first modules by measured `os.getenv` count).
- **Wave 4 prep:** the `voice_tts.py` split execution packet exists — [`docs/architecture/voice-tts-split-plan.md`](architecture/voice-tts-split-plan.md) (6 sequenced verbatim-move seam PRs, symbols verified, every step replay-gated; executable cold by a mid-tier agent).

---

## 🗂️ Backlog — pinned & sequenced (from Jason, 2026-06-23)
*Agent/tooling readiness for remote work. Each is a tracked item; do them in order, don't bounce.*

- [x] **1. Audit Omnigent** ✅ — done in #774. Fix ✅ — #792 committed the read-only mounts + container-relative `.mcp.json`; **applied + verified live 2026-06-26** (container recreated; serena+codebase-memory+opensrc resolve in-container).
- [x] **2. Audit + wire Claude Code** ✅ — #774 audit; #787 added `CLAUDE.md`→`@AGENTS.md` and committed the previously-ignored `.mcp.json` (Serena + codebase-memory).
- [x] **3. Audit + wire Codex** ✅ — #774 audit; #788 added repo-local `.codex/config.toml` registering both MCP servers (Codex reads `AGENTS.md` natively).
- [ ] **4. Enforce tool use across ALL agents** 🔨 in progress — AGENTS.md "start-of-task checklist" making code-intel/opensrc/Greptile/DOX non-optional + verifying each entrypoint references the hub.
- [x] **5. Populate opensrc with our reference repos** ✅ — #773 (llama.cpp, Serena, codebase-memory, OKF; confirmed ag-ui, MemPalace; mapped internal sources).
- [ ] **6. Verify the Dox `AGENTS.md` system is referenced by each agent** — folded into #4 (Cursor pointer ✅ #786; Claude ✅ #787; Codex native ✅; Omnigent pending the container fix).
- [x] **7. Stand up this command center** ✅ — `docs/IDEAS.md` + `docs/PLANS.md` + wired into `AGENTS.md` (landed via the VISION train).
- [ ] **8. Research a richer ideas-board surface** (see IDEAS.md) — 📝 not started.

---

## ✅ Done
- **Security & reliability hardening wave (2026-06-29 → 2026-07-02, ~30 PRs #909–#944):** WebSocket Origin validation (CSWSH, #933) + push-WS lifecycle/fanout hardening (#917/#922), panel authz holes closed (#921/#873), SSRF + argv-exec guards (#909), bounded outbound fetch bodies (#931), auth session invalidation after password rotation (#938) + query-string PII log leak (#926), skill-name path-traversal sandbox (#930), nginx proxy headers/CSP (#925), memory edit data-loss/recall re-embed/unbounded growth fixes (#934), Alembic-managed consolidation state (#929), timezone-correct dates across MCP/frontend (#914/#919/#923), exact integer-cents money (#924), deploy readiness gating (#940), Multica outage observability (#932), vacuous-test strengthening (#913).
- **Gated zoe-data deploy helper (2026-06-29):** added a host-run one-liner wrapper that checks the live tree, memory headroom, explicit restart confirmation, and post-deploy health while delegating pull/restart/rollback to the blessed deploy script.
- **WebSocket lifecycle hardening (2026-06-29):** fixed streaming push WS cleanup leaks from malformed `catchup:` and half-open idle sockets; all `main.py` push endpoints now share guarded catchup parsing, idle receive deadlines, and `finally` broadcaster cleanup.
- **Config/deploy health hardening (2026-06-28):** kept `/health` as backward-compatible liveness and added `/readyz` for real Gemma 4 E4B, Moonshine, and TTS readiness; removed faster-whisper/whisper warm-resurrection paths; moved dependency health out of deploy liveness, tightened auth DB health dependency, and raised nginx API upload size.
- **Repo cleanup + lock-in (2026-06-24):** purged `docs/archive` (84MB→1.9MB) + a 69MB untracked backup; added `docs/CANONICAL.md` (single live/dead authority) + `test_canonical_invariants.py` (CI fails if a rock is swapped) + wired CANONICAL into AGENTS.md READ-FIRST; fixed manifest E2B→E4B drift. (#777)
- **Module retirement (2026-06-24):** retired `orbit`/`agent-zero`/`jag-board`/`questionable-decisions` (code + nginx/UI/compose wiring); `jag-board` + `questionable-decisions` preserved (private repos + local archive); `zoe-music` kept, pinned for Music Assistant migration. (#779)
- **Agent-readiness wave (2026-06-24):** opensrc repos (#773), agent audit (#774), Claude Code (#787) / Codex (#788) / Cursor (#786) all wired to load the rules + code-intel MCP.
- **Samantha memory 1a + 1b (2026-06-24):** idle-consolidation engine (#771) + per-turn user (#775), behind a flag.
- **Voice + memory hardening (2026-06-23):** capture/first-audio/recall fixes, write-quality gate, junk cleanup, identity-from-auth. (See PR history; memory `project-mempalace-deep-dive`.)
- **Command center stood up** (this doc + IDEAS.md + VISION.md).
