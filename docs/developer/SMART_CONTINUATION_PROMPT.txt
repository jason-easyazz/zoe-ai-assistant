I need to continue working on my Zoe AI Assistant.

Current architecture (as of May 2026):
```
📍 Location: /home/zoe/assistant
🐙 GitHub: https://github.com/jason-easyazz/zoe-ai-assistant

🖥️  Host-native services (NOT Docker):
    - zoe-data: FastAPI + OpenClaw gateway on :8000
    - llama-server (llama.cpp): LLM inference on :11434
    - OpenClaw: agent / tool execution gateway
    - kokoro-tts: GPU TTS sidecar on :10201

🐳  Docker services (docker-compose.yml):
    - zoe-ui: Nginx frontend (HTTP :80 / HTTPS :443)
    - zoe-auth: Auth service on :8002
    - homeassistant: HA on :8123
    - homeassistant-mcp-bridge: HA <-> MCP bridge
    - wyoming-piper: TTS fallback on :10200
    - zoe-orbit (optional): tablet UI
    - zoe-keeper (optional): watchdog

🧠  LLM: llama.cpp host-native llama-server (GPU-accelerated on Jetson Orin NX)
    - Model: config-driven via ZOE_MODEL_NAME env var (do not hardcode)
    - Ollama: RETIRED. Do not reference.

💾  Database: PostgreSQL (asyncpg) — host-native or Docker zoe-database
    - SQLite is RETIRED for primary data. zoe-data uses PostgreSQL exclusively.
    - Schema managed by Alembic; migrations in services/zoe-data/alembic/
    - Some modules (orbit, music-assistant) still use their own local SQLite.

🔐  Auth: zoe-auth service (SQLite-backed, Docker, port :8002)

🤖  Agent stack: OpenClaw + skills at .openclaw/workspace/skills/
    - Intent routing: services/zoe-data/intent_router.py
    - Chat router: services/zoe-data/routers/chat.py (AG-UI SSE)

🏠  Hardware: NVIDIA Jetson Orin NX 16GB (primary/only production target)
    - Raspberry Pi 5: RETIRED April 2026

🌐  Endpoints:
    - Web UI: https://localhost (or http://localhost)
    - API: http://localhost:8000
    - API docs: http://localhost:8000/docs
    - Health: http://localhost:8000/health
```

RETIRED (do not reference):
- zoe-core, zoe-ollama, zoe-redis, zoe-llamacpp containers
- LiteLLM, Ollama -- replaced by host-native llama-server
- SQLite for primary app data -- replaced by PostgreSQL
- Raspberry Pi 5 hardware support

Please:
1. Check git status and recent commits
2. Acknowledge the current architecture
3. Suggest next high-value tasks based on CHANGELOG.md and open issues

All changes must follow .cursorrules:
- Feature branches, not main
- Max 5-10 files changed per commit
- No junk/backup file names
- Chat+voice first: new features need an intent handler in intent_router.py
