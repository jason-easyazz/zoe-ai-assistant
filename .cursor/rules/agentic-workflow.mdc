---
description: Zoe agentic development workflow with codebase-memory, opensrc, Hermes, Greptile, and verification
alwaysApply: true
---

# Agentic Workflow

For Zoe architecture or cross-module work, start with the codebase-memory MCP (re-indexed on demand) for code-graph and architecture context before broad source searches. Graphify is retired; do not rely on any `graphify-out/` artifact.

For third-party libraries, prefer `opensrc` or upstream docs before guessing API behavior. Keep external source caches outside the repo, under `~/.opensrc/repos/`. Treat new package adoption cautiously: prefer mature packages, check source/maintenance history, and avoid adding dependencies for code less than about 14 days old unless the user explicitly accepts the risk.

For agent delegation, use Hermes. This includes planning, code repair, architecture, PR review loops, Greptile feedback, Multica board repair, Zoe knowledge refresh, and browser work through Zoe's CloakBrowser tools. OpenClaw is being FULLY RETIRED (operator decision 2026-07-22, docs/architecture/multica-executor-migration.md §5) — never route work to it; its runtime + builder intents are to be deleted via gated PRs, with backups already in docs/knowledge/operator-skills/.

Plan first for broad or risky work. Keep changes surgical. Prefer existing Zoe patterns, avoid backup/old/new file copies, and do not create parallel routers or duplicate feature files.

Build the minimal working feature first. After it works, run a cleanup pass for duplicated runtime mechanics: repeated provider calls, parsing, validation, command execution, payload transforms, or business logic. Keep domain policy in routes/actions/intents and reusable mechanics in service-layer helpers.

For Hermes-backed engineering work, prefer the local Hermes skills `zoe-engineering`, `source-code-context`, `code-structure-cleanup`, `github-greptile-loop`, `zoe-graphify`, and `zoe-status-refresh` when available. These are operator-level Hermes skills under `~/.hermes/skills`, not Zoe runtime user-facing skills.

Use the small-PR loop for reviewable work: split large changes, push a focused PR, let Greptile review independently, then use Zoe's Greploop guard. Greptile MCP status/comment reads are fine (`get_merge_request`, `list_merge_request_comments`), but trigger/re-trigger reviews only through `scripts/maintenance/run_greploop_guard.sh --pr N --once` so repo-wide cooldowns, running-review checks, and shared state protect other agents. For heavier repair loops, delegate to Hermes `github-greptile-loop`. Read the PR diff before fixing comments and stop to split if the PR is too large for reliable review.

`main` is protected. Do not push directly, bypass branch protection, or use administrator merges unless the operator explicitly asks for that emergency path. If GitHub blocks a green PR because repo auto-merge is disabled or another policy requires human action, report the blocker and keep the PR ready rather than forcing it.

Branching is trunk-based: one short-lived branch per issue off `origin/main`, no permanent `develop`. Use `~/.worktrees/<slug>` for agent work; do not repoint the live checkout to feature branches. After merge, remove the worktree; prune stale ones with `scripts/maintenance/prune_worktrees.sh` (dry-run first).

Verify from a user perspective before commit: focused tests, `validate_structure.py`, `validate_critical_files.py`, live `/health` and `/api/system/status`, and any relevant systemd timers or MCP tools.
