fastapi==0.141.1
# uvicorn is CAPPED AT 0.49.x ON PURPOSE — do not "bump to latest".
# 0.50.0 flipped `--ws auto` from the legacy `websockets` protocol to
# `websockets-sansio` whenever websockets is installed. main.py serves NINE
# @app.websocket routes and one of them is `/ws/voice/`, so that flip swaps the
# WebSocket implementation underneath the live voice path with no code diff.
# Verified on-box (py3.10): 0.49.0 still resolves `auto` -> websockets_impl.
# Moving to >=0.50 is a deliberate, replay-gated change, not a currency bump.
uvicorn[standard]==0.49.0
pydantic==2.13.4
aiosqlite==0.22.1
asyncpg>=0.29.0
alembic>=1.13.0
psycopg2-binary>=2.9.0
python-multipart==0.0.32
httpx==0.28.1
aiohttp==3.14.3
# websockets 16.1.1 is the LAST RELEASE THAT INSTALLS ON PYTHON 3.10 — 17.0
# requires >=3.11 and this host is 3.10.12. Do not raise this pin without first
# moving the host's Python. The client surface we use (`websockets.connect` with
# open_timeout= / max_size=) is unchanged from 14.x, and uvicorn's legacy server
# impl still finds `websockets.server.WebSocketServerProtocol` here (deprecated
# alias, present through 16.x).
# NOTE: since 15.0 a client `connect()` honours OS/env proxy settings
# automatically. Nothing sets HTTP_PROXY/ALL_PROXY on this box today, but if that
# ever changes, the local Music-Assistant and LiveKit sockets must pass proxy=None.
websockets==16.1.1
python-jose[cryptography]==3.5.0
PyJWT==2.13.0
passlib[bcrypt]==1.7.4
ag-ui-protocol==0.1.19
python-json-logger>=4.1.0

# ── Vector memory store ───────────────────────────────────────────────────
# BOTH pins are exact, and the chromadb one is the load-bearing half.
#
# mempalace 3.3.1 declares only `chromadb>=0.5.0`, so without an explicit pin a
# resolver is free to install chromadb 1.5.x against a 0.6.x on-disk palace. That
# is not a clean upgrade: mempalace's own changelog documents `_fix_blob_seq_ids`
# misreading chromadb 1.5.x's sysdb-10 BLOB format as legacy 0.6.x big-endian
# BLOBs, causing **silent drawer-write drops** on palaces migrated in that state
# (recovery is `mempalace repair --mode max-seq-id`). Silently losing writes is
# strictly worse than the 2026-07-31 crash-loop, which at least announced itself.
#
# Upgrading mempalace to 3.6.0 FORCES that migration (it requires
# chromadb>=1.5.4,<2), so the two move together or not at all. 3.6.0 does carry
# real fixes for our exact failure — HNSW divergence detection before opening
# ChromaDB, and derived-index damage distinguished from data loss — so it is
# worth doing, but only via: copy the palace, `mempalace migrate` in a throwaway
# venv, then reconcile row counts against a `scripts/maintenance/export_memory_store.py`
# export and confirm `memory_recall_probe` still self-recalls. Not in place.
#
# Live-verified combination (2026-08-02, 3507 drawers + 17686 audit rows):
mempalace==3.3.1
chromadb==0.6.3

# ── Voice dependencies (Jetson/server-side) ───────────────────────────────
# `numpy<2` guards MORE than the voice path — do not relax or relocate it as
# part of a voice-only change. Jetson torch/ctranslate2 wheels fail at runtime
# on NumPy 2.x, AND chromadb 0.6.3 declares a bare unbounded `numpy`, so this
# ceiling is the only thing stopping a memory-store install from pulling NumPy
# 2.x (verified 2026-08-02: `mempalace==3.3.1 chromadb==0.6.3` alone resolves to
# numpy 2.2.6; adding this line pins it back to 1.26.4). NumPy 2 is a C-ABI
# break, and the packages compiled against 1.x here are kokoro, moonshine-voice,
# chroma-hnswlib, ctranslate2, soundfile, soxr and Resemblyzer — i.e. both
# canonical rocks plus the vector index. On aarch64 the realistic failure is
# "no matching wheel" and a dead voice path.
numpy<2
# Cloud TTS fallback (fast, AU Neural voice)
edge-tts>=6.1.12
# Primary offline TTS is the Kokoro PyTorch sidecar (scripts/setup/kokoro_sidecar.py,
# port 10201) — NOT an in-process dep here. zoe-data reaches it over HTTP and falls
# back to edge/espeak if it is down (see ZOE_TTS_MODE in .env.example).
# STT primary: Moonshine v2 (MEDIUM_STREAMING) — accurate on real room audio, CPU/ONNX
moonshine-voice
# ── Declared-but-previously-implicit runtime deps ─────────────────────────────
# Both are imported DIRECTLY by live zoe-data code but were in no requirements
# file, so they only ever arrived as somebody else's transitive dependency
# (onnxruntime rode in on chromadb's `onnxruntime>=1.14.1`; transformers rode in
# on the kokoro/faster-whisper stack). That makes every resolve dishonest: drop
# the package that happened to carry them and the voice path breaks at runtime,
# not at install time.
#   onnxruntime — voice_turn.py (Smart Turn v3 endpointing), voice_vad.py
#   transformers — voice_turn.py (WhisperFeatureExtractor)
# Pinned AT THE CURRENTLY-INSTALLED VERSIONS to record reality, NOT to upgrade;
# these are deliberately not bumped here. (onnxruntime's own py3.10 ceiling is
# 1.24.3, so there is headroom when someone wants it.)
onnxruntime==1.23.2
transformers==5.5.0
# Tier-1 semantic router: bge-small ONNX embeddings (~7-30ms/query, CPU) for
# fuzzy domain classification between the regex fast-path and the Pi brain.
fastembed
# SetFit router head (shadow mode, ZOE_ROUTER_HEAD): loads
# models/router_head_logreg.joblib — pins MUST match the versions the artifact
# was trained with (labs/setfit-router/requirements.txt).
scikit-learn==1.7.2
joblib==1.5.3
# Speaker diarization for ambient memory (Jetson CUDA only)
# VAD for barge-in and ambient capture (server-side copy; Pi uses same package)
# Proactive engine scheduler.
#
# HELD AT 3.10.4 DELIBERATELY — 3.11.x is a DATA hazard, not just a bump.
# proactive/scheduler.py runs a SQLAlchemyJobStore (PostgreSQL), which PICKLES
# each job's trigger. Under 3.10.4 (+ tzlocal 2.x) those triggers carry **pytz**
# tzinfo objects. 3.11 deprecates pytz timezones and drops pytz from its
# dependencies entirely (it also moves to `tzlocal>=3.0`, which is why
# .github/workflows/validate.yml pins `tzlocal>=2,<3` alongside this line — the
# two move together or CI installs an unsatisfiable pair).
#
# The failure is SILENT AND DESTRUCTIVE: jobstores/sqlalchemy.py `_get_jobs()`
# wraps `_reconstitute_job()` in `except BaseException`, logs
# "Unable to restore job -- removing it", and DELETES the row. So an
# already-persisted reminder or Multica autopilot whose pytz trigger no longer
# unpickles is not skipped — it is permanently erased.
#
# Upgrading means draining/re-registering the jobstore (or keeping pytz
# installed explicitly) and re-pinning tzlocal in validate.yml. Not in place.
APScheduler==3.10.4
SQLAlchemy>=2.0,<3.0
# Push notifications
pywebpush>=2.0
py-vapid>=1.8
# Speaker identification (voice enrollment and identify endpoints)
resemblyzer>=0.1.3
# LiveKit WebRTC server-side agent (joins room, STT+LLM+TTS, publishes audio)
livekit>=0.14
# aiortc and av move TOGETHER: aiortc 1.14.0 caps `av<17`, so av cannot advance
# without it (1.15.0 widens the cap to `av<18`).
# av is held at 17.1.0 because 18.0.0 requires Python >=3.11 (host is 3.10.12).
# av 17 bundles FFmpeg 8.1.1 and drives livekit_aiortc.py's AudioResampler, so it
# was checked numerically rather than by changelog: resampling 48k stereo s16 ->
# 16k mono s16 (the exact config that file uses) is BIT-IDENTICAL between 16.1.0
# and 17.1.0, and deterministic across runs. Both ship cp310 aarch64 wheels.
aiortc==1.15.0
av==17.1.0
# A2A federation + skill discovery + web search
ddgs>=6.0
cloakbrowser>=0.3.28
PyYAML>=6.0
# CloakBrowser — stealth Chromium for bot-blocked web search fallback (optional).
# Install with: pip install cloakbrowser
# The binary auto-downloads (~200MB) to ~/.cloakbrowser/ on first launch.
# cloakbrowser>=0.3.25
segno
